//map the seed( create a card for images)
//need to send state here

import React from 'react'

const FavoriteCard = ({user, favorites, removeFavorite, meets}) => {
    //console.log(meets)
    return (
        <div>
            {/* {meets.map((place) => (
                <img src={place.image_url} alt='Places'></img>
            ))} */}
        </div>
    )
}

export default FavoriteCard